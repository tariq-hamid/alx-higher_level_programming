#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    """Function that adds 2 tuples.

    Args:
        tuple_a: first tuple
        tuple_b: second tuple

    Returns:
        Nothing
    """